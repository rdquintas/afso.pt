{% extends 'partials/base.html.twig' %}

{% block content %}
<section class="page noticia">
	<h1>{{ page.title }}</h1>
	<p class="date">{{ page.date|date("d/M/Y") }}</p>
	{{ page.content }}
</section>
{% endblock %}

